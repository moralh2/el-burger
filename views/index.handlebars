<h1>All of the Burgers</h1>

<h3>Choose which burger you wish to DEVOUR</h3>

<div class="container">
    <div class="row">

        {{#each burgers}}

        <div class="card w-25" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{id}}: {{name}}</h5>
                <h6 class="card-subtitle mb-2 text-muted">Devoured: {{devoured}}</h6>
                <a href="#" class="card-link">Eat Me!</a>
            </div>
        </div>

        {{/each}}
    </div>

</div>




{{!-- <form action="/" method="POST">
    <textarea type="text" name="task"></textarea>

    <button type="submit">Submit</button>
</form> --}}